version: '3.8'

services:
  app:
    build: .
    container_name: tdconnect-app
    restart: always
    ports:
      - "3001:3000" # Expose local port 3001 to access the app on http://localhost:3001
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=RemplissezAvecUneCleSecreteAleatoireEtLongue
    volumes:
      - ./database.sqlite:/app/database.sqlite
